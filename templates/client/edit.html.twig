{% extends 'base.html.twig' %}

{% block title %}Modifier Client{% endblock %}

{% block body %}
    <div class="max-w-2xl mx-auto mt-10 p-6 bg-white shadow-xl rounded-2xl">
        <h1 class="text-2xl font-bold text-purple-700 mb-6">Modifier le client</h1>

        {{ include('client/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="mt-6 flex items-center justify-between">
            <a href="{{ path('app_client_index') }}"
               class="inline-block px-5 py-2 bg-gray-200 hover:bg-gray-300 text-gray-800 rounded-xl shadow transition">
                ⬅️ Retour à la liste
            </a>

            <div>
                {{ include('client/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
